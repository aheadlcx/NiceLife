<manifest package="me.aheadlcx.health"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        >
        <activity android:name=".ui.health.HealthNewsListAct">

        </activity>

        <activity android:name=".ui.health.detail.HealthNewsDetailActivity"></activity>
        <activity android:name=".test.TestListAct"></activity>
        <activity android:name=".test.TestListSyncAct"></activity>
        <activity android:name=".test.GuideAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>
